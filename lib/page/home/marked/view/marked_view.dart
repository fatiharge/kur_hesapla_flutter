import 'package:flutter/material.dart';

class MarkedView extends StatelessWidget {
  const MarkedView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('CalculatorView'),
      ),
    );
  }
}
